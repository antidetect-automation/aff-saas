# Local tooling only — NEVER commit real secrets.
# Worker production secrets: `npx wrangler secret put NAME`

# Required for Telegram bot (already on Worker)
# BOT_TOKEN=
# STATS_KEY=
# DIGEST_CHAT_ID=-1004445803393

# Required for post-Telegram GitHub Pages publish
GITHUB_TOKEN=ghp_your_pat_with_contents_write_on_aff_saas

# Optional overrides (defaults shown)
# GITHUB_OWNER=antidetect-automation
# GITHUB_REPO=aff-saas
# GITHUB_BRANCH=main
